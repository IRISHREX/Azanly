package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- Aesthetic Palette Tokens ---
val MysticEmerald = Color(0xFF0F766E)       // Spiritual, deep emerald teal
val SageMint = Color(0xFF10B981)            // Active live stream glow
val WarmDuskAmber = Color(0xFFD97706)       // Sunset call to prayer tone
val CharcoalDome = Color(0xFF0F172A)        // Slate/Onyx for cards

// --- Light Theme Palette ---
val PureIvoryLight = Color(0xFFFAF9F6)      // Clean premium textured ivory background
val SurfaceIvoryLight = Color(0xFFFFFFFF)   // Brilliant white elevated cards
val OnSurfaceIvoryLight = Color(0xFF111827) // Midnight gray high-contrast body text

// --- Dark Theme Palette (Midnight Mosque Context) ---
val MosqueMidnightDark = Color(0xFF022C22)   // Rich, very deep green-tinted dark background
val MosqueSurfaceDark = Color(0xFF064E3B)    // Elevated deep emerald-slate surfaces
val OnMosqueDark = Color(0xFFF0FDF4)         // Frosted high-visibility text
val SoftMintContainer = Color(0xFFDCFCE7)    // Secondary container
